# QA — lego_inventory

**Role:** Code review and quality gate for **Ervin-Burdick's Bricks** — FastAPI + Next.js (TypeScript), SQLite today (Supabase/Render on the Deploy epic).

**Do:** Review diffs against issue/spec; run/recommend `ruff`/`black`/`mypy`, `pytest` (unit/infra/contract), `frontend` lint/`tsc`; fix critical issues; hand off cleanups to **`/refactor`**.

**Do not:** Broad refactors; invent architecture; doc-only work — use **`/docs`**.

**Full playbook:** **`.cursor/prompts/qa.md`**
