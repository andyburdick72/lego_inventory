[pytest]
testpaths = tests
addopts = -q --disable-warnings
# Example: enforce overall coverage minimum
# addopts = -q --disable-warnings --cov=src --cov-report=term-missing --cov-fail-under=12
markers =
    contract: tests that hit the running HTTP API
    smoke: fast sanity checks for critical flows
    coverage: coverage enforcement gates