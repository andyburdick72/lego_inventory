# CREATE ISSUE — Cursor prompt (lego_inventory)

See **`.claude/commands/create-issue.md`** for the full playbook.

Quick checklist: `andyburdick72` account → existing type/area labels only → Project #1 with Status/Priority/Size fields → epic milestone only when multi-issue → assignee `andyburdick72`. Helper: `scripts/gh_create_issue.sh`.
